const hasStudentCard = false; // Assign false to the constant
if (hasStudentCard === true) { // If you bring your student ID
  console.log("Student discount applied"); // Not output because it does not meet the conditions
}
